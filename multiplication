#include<stdio.h>
#include<conio.h>
void main()
{
	int a,b,multiply;
	clrscr();
	printf("Enter the first number:");
	scanf("%d",&a);
	printf("Enter the second number:");
	scanf("%d",&b);
	multiply=a*b;
	printf("The multiplication of the two numbers=%d",multiply);
	getch();
}
