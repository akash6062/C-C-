#include<stdio.h>
void prime(int num)
void main()
{
int num;
printf(“Enter a num:”);
scanf(“%d”,&num);
prime(int  sum)
x=prime(int sum);
grtch();
}
int prime(int sum)
int i,num,flag=0;
for(i=2;i<=num/2;i++)
if(num%2==0)
flag=1;
break;
if(flag==0)
{
Printf(“No is prime no:”);
}
else
{
printf(“No is prime no:”);
}
