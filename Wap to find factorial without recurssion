#include<stdio.h>
	#include<conio.h>
	int main()
	{
		clrscr();	
		int n,i, fact=1;
		printf("Enter a positive number  \n");
		scanf("%d",&n);
		for(i=1 ; i<=n; i++)
		{
			fact= fact*i;					
		}
		printf("Factorial of %d = %d \n", n, fact);
		return 0;
		getch();
	}
